name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Run tests
        run: go test ./...

      - name: Build binaries
        run: |
          mkdir -p dist
          GOOS=darwin GOARCH=arm64 go build -ldflags "-s -w" -o dist/arxs-darwin-arm64 .
          GOOS=darwin GOARCH=amd64 go build -ldflags "-s -w" -o dist/arxs-darwin-amd64 .
          GOOS=linux GOARCH=amd64 go build -ldflags "-s -w" -o dist/arxs-linux-amd64 .
          GOOS=linux GOARCH=arm64 go build -ldflags "-s -w" -o dist/arxs-linux-arm64 .
          GOOS=windows GOARCH=amd64 go build -ldflags "-s -w" -o dist/arxs-windows-amd64.exe .

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: dist/*
